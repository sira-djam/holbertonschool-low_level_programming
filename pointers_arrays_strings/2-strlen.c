#include "main.h"

/**
  * _strlen - afficher longueur d'une chaine de caractere
  * @s: pointer
  * Return: always
  */

int _strlen(char *s)
{
int longueur = 0;
		while (longueur[s] != '\0')
		{
			longueur++;
}
		return (longueur);
}
