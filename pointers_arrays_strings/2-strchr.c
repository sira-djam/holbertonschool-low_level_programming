#include "main.h"
#include <stdio.h>
/**
  * *_strchr - localisation d'un caracter dans une string
  * @s: string
  * @c: le caractere
  * Return: always
  */
char *_strchr(char *s, char c)
{
	for (*s = 0 ; *s != '\0' ; s++)
		if (*s == c)
		{
			_putchar('s');
		}
			return (0);
}
