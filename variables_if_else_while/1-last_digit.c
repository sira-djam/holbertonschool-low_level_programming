#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - entry point
 * returne always
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
if (last digit of n>5)
{
printf("%d and is greater than 5\n" , n) ;
}
if (last digit of n=0)
{
printf("%d and is 0\n" , n) ;
}
if (last difit of n<6 ,
	return (0);
}
