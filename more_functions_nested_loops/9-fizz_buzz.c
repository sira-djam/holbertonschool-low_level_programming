#include<stdio.h>
/**
  * fizz_buzz - impriméselon si multiple ou divisible les mots fizz ou buzz ou fizz buzz
  * Return always
  */
fizz_buzz (int a)
{
	for (a = 1 ; a <= 100 ; a++)
{
	if (a % 15 == 0)
	{
		printf(FizzBuzz);
	}
	else if (a % 5 == 0)
	{
		printf(Buzz);
	}
	else if (a % 3 == 0)
	{
		printf(Fizz);
	
	}
	else
	{
		printf(a)
}
}
}
