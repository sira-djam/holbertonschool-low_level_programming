#include "main.h"
/**
  * more_functions-nested_loops - imprimer la suite de nombre de 0 a 14 10 fois
  * Return always
  */
void more_numbers(void)
{
	int r;
	int n;
	r = 0;

	while (r < 10)

	for (n >= 0 ; n <= 10 ; n++)
	
	for (n > 10 ; n <= 14; n1++)
	{
		_putchar(n);
	}
	_putchar('\n');
}
