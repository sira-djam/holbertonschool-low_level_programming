#include "calc.h"
/**
 *
 */

int main(argc, *argv[])

int (*oprt)(int, int)

if (argc != 4)
{
printf("error\n");
exit (98);
}
if (!oprt)
{
printf("error\n")
exit(99);
}
printf("%d\n", oprt(atoi(arv[1], atoi(argv[3])));
return (0);
}
